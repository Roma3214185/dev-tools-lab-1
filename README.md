# DataInputService Tests

**DataInputService Tests** — це набір модульних тестів для перевірки коректності введених користувачем даних.  
Вони покривають різні аспекти валідації та забезпечують надійність функцій:

- **Ім’я користувача** — перевірка довжини, контрольних символів та допустимих символів, UTF-8.
- **Електронна пошта** — перевірка формату, плюс-аліасів, піддоменів, IP-літер, довжини локальної частини.
- **Пароль** — перевірка довжини, пробілів, контрольних символів, Unicode та недопустимих символів.
- **Теги** — перевірка символів, довжини, подвійних підкреслень та першого символу.

Мета тестів — гарантувати, що модуль `DataInputService` коректно валідуює всі типи введених даних.

---

## 🔧 Основні функції тестів

| Функція | Призначення |
|---------|-------------|
| `nameValidDetailed(name, cfg)` | Перевіряє довжину імені, наявність контрольних символів та недопустимих символів. |
| `passwordValidDetailed(password, cfg)` | Перевіряє довжину пароля, пробіли, контрольні символи, Unicode та недопустимі символи. |
| `tagValidDetailed(tag, cfg)` | Перевіряє, що тег починається з літери або цифри, не містить подвійних підкреслень і має правильну довжину. |
| `emailValidDetailed(email, cfg)` | Перевіряє формат електронної пошти, плюс-аліаси, піддомени, IP-літери, довжину локальної частини. |
| `validateUserInput(input, cfg)` | Виконує комплексну перевірку всіх полів структури `UserInput`. |

---

## 🚀 Приклад використання тестів

```cpp
#define CATCH_CONFIG_MAIN
#include <catch2/catch_all.hpp>
#include "../DataInputService.h"

using namespace DataInputService;

TEST_CASE("Example test", "[example]") {
    Config cfg;
    cfg.kMinLenOfName = 1;
    cfg.kMaxLenOfName = 10;

    auto result = nameValidDetailed("John", cfg);
    REQUIRE(result.valid);
}
