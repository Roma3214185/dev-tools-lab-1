# DataInputService

**DataInputService** — це невеликий модуль для перевірки коректності введених користувачем даних.  
Він забезпечує базову валідацію таких типів даних:

- **Ім’я користувача** — перевірка довжини.
- **Електронна пошта** — перевірка формату та домену.
- **Пароль** — перевірка довжини та допустимих символів.
- **Теги** — перевірка символів, довжини та відсутності подвійних підкреслень.

Мета програми — забезпечити надійну перевірку вхідних даних перед їх подальшою обробкою або збереженням.

---

## 🔧 Основні функції

| Функція | Призначення |
|----------|--------------|
| `nameValid(name)` | Перевіряє, чи довжина імені користувача знаходиться в межах допустимих значень. |
| `emailValid(email)` | Перевіряє формат електронної пошти та домен (`kEmailDomain`). |
| `passwordValid(password)` | Перевіряє довжину та дозволені символи пароля. |
| `tagValid(tag)` | Перевіряє, що тег починається з літери або цифри, не містить подвійних підкреслень і має правильну довжину. |

---

## 🚀 Приклад використання

```cpp
#include "DataInputService.h"
#include <iostream>

int main() {
    std::string email = "user@example.com";
    std::string password = "Strong_Pass1!";
    std::string tag = "valid_tag";

    if (DataInputService::emailValid(email)) {
        std::cout << "Email is valid ✅" << std::endl;
    }

    if (DataInputService::passwordValid(password)) {
        std::cout << "Password is valid ✅" << std::endl;
    }

    if (DataInputService::tagValid(tag)) {
        std::cout << "Tag is valid ✅" << std::endl;
    }

    return 0;
}
